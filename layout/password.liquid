{% capture layout %}
  {% render 'layout' | split: '{ content_for_header }' %}
{% endcapture %}

{% capture body %}
  {{ layout | last }}
{% endcapture %}

{{ layout | first }}
  {{ content_for_header }}
{{ body | split: '{ content_for_layout }' | first }}
  {{ content_for_layout }}
{{ body | split: '{ content_for_layout }' | last }}
